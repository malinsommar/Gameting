const homeView = "/home"
const contactView = "/contact"
const matchRequestView = "/matchRequest"
const matchView = "/match"
const messagesView = "/messages"
const profileView = "/profile"
const settingsView = "/settings"
const signInView = "/"
const signUpView = "/signUp"
const newAccountView = "/newAccount"
const forgotPassword = "/resetPassword"

export default {
    homeView,
    contactView,
    matchRequestView,
    matchView,
    messagesView,
    profileView,
    settingsView,
    signInView,
    signUpView,
    newAccountView,
    forgotPassword
}